# Claude code pin (auto-generated)

> **Do not edit by hand.** Regenerated by `scripts/update_claude_pin.py` or GitHub Actions.
> Generated: 2026-05-22 02:29:09 UTC

## Instructions for Claude

1. Read **this file** from GitHub `main` (single entry point):
   `https://raw.githubusercontent.com/skcKenneth/acvrp-co2/fe8cd39b33f0c7b603a6e6635f9fb7320cdb9dfb/docs/CLAUDE_PIN.md`
   Or browse: https://github.com/skcKenneth/acvrp-co2/blob/main/docs/CLAUDE_PIN.md
2. **Ignore** code from earlier chat turns, local paths (`C:\...`, `D:\...`), or old zips.
3. Use **only** the pinned commit and raw URLs in the next section for NCO / CO2 / Methods work.
4. Reply first with: `HEAD used = fe8cd39b33f0c7b603a6e6635f9fb7320cdb9dfb` (`fe8cd39`) and pass/fail for checks 1–5 below.
   If raw content does not match this SHA, reply **STALE** and stop.

## Pinned commit

| Field | Value |
|-------|-------|
| Full SHA | `fe8cd39b33f0c7b603a6e6635f9fb7320cdb9dfb` |
| Short | `fe8cd39` |
| Branch | `main` |
| Compare on GitHub | https://github.com/skcKenneth/acvrp-co2/commit/fe8cd39b33f0c7b603a6e6635f9fb7320cdb9dfb |
| Latest commits | https://github.com/skcKenneth/acvrp-co2/commits/main |

## Verification checks (must pass on pinned SHA)

1. BatchedInstances includes `co2_per_arc`; `collate_instances` returns `co2_per_arc=co2_abs`.
2. `model.py` uses `batch.co2_per_arc` — no `_reconstruct_co2_per_arc` function.
3. POMO first step: forced-first `step_lp` uses `.detach()`.
4. `baseline_am.py` uses `batch.co2_per_arc` — no `ACVRPPolicy as _AP` CO2 reconstruction.
5. `trainer.py` defines `_infer_legacy_arch()` and uses it in `load_checkpoint`.

## Source files (raw, commit-pinned)

- https://raw.githubusercontent.com/skcKenneth/acvrp-co2/fe8cd39b33f0c7b603a6e6635f9fb7320cdb9dfb/src/nco/instance.py
- https://raw.githubusercontent.com/skcKenneth/acvrp-co2/fe8cd39b33f0c7b603a6e6635f9fb7320cdb9dfb/src/nco/model.py
- https://raw.githubusercontent.com/skcKenneth/acvrp-co2/fe8cd39b33f0c7b603a6e6635f9fb7320cdb9dfb/src/nco/baseline_am.py
- https://raw.githubusercontent.com/skcKenneth/acvrp-co2/fe8cd39b33f0c7b603a6e6635f9fb7320cdb9dfb/src/nco/trainer.py

## Regenerate (developers)

**After `git push` on either machine** (optional if CI already updated this file):

```cmd
cd <repo-root>
conda activate base
scripts\update_claude_pin.bat
```

Windows without conda: ``.\scripts\update_claude_pin.ps1``

Linux / GitHub Actions: ``python scripts/update_claude_pin.py``

Then: ``git add docs/CLAUDE_PIN.md`` && commit && push (optional if CI updated the file).

Pin a specific commit: ``scripts\update_claude_pin.bat --sha <40-char-sha>``

## One-line prompt to paste into a new Claude chat

```
Read https://raw.githubusercontent.com/skcKenneth/acvrp-co2/main/docs/CLAUDE_PIN.md — follow it exactly (pinned commit fe8cd39). Then: [YOUR TASK]
```
